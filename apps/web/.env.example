# Alchemy Prices API key, for showing slot figures in dollars.
# Server-side only: it is read by /api/price and never reaches the browser.
# Without it the app works and simply shows no dollar amounts.
ALCHEMY_API_KEY=
