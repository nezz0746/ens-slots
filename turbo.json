{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": { "outputs": ["out/**", "cache/**", "dist/**"] },
    "test": { "dependsOn": ["build"], "cache": false },
    "fmt": { "cache": false }
  }
}
